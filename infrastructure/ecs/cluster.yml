Resources:
  ECSGraphQLCluster:
    Type: 'AWS::ECS::Cluster'
    Properties:
      ClusterName: ECSGraphQLCluster

Outputs:
  ECSGraphQLClusterArn:
    Value:
      Ref: ECSGraphQLCluster
    Export:
      Name: ECSGraphQLClusterArn